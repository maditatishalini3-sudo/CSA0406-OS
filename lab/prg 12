#include <stdio.h>
int main()
{
    int i;
    for(i = 1; i <= 5; i++)
    {
        printf("Philosopher %d is thinking\n", i);
        printf("Philosopher %d is eating\n", i);
        printf("Philosopher %d released forks\n", i);
    }
    return 0;
}
